#ifndef CONSTANTS_H_SENTRY
#define CONSTANTS_H_SENTRY

enum { square_width = 2 };

enum { blocks_count = 7 };
enum { tetromino = 4 };

enum { field_height = 20, field_width = 10 };

enum { drop_delay = 650 };

#endif
